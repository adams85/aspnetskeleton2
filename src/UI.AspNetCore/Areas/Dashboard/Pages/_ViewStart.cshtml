@attribute [AssociatedAssemblyName(ApplicationConstants.AssemblyNamePrefix + ".UI.AspNetCore")]

@model UI.Models.Layout.ILayoutModelProvider<UI.Models.Layout.LayoutModel>

@{
    Model.Layout.Initialize(ViewContext, T);

    Layout = Model.Layout.LayoutName ?? "_DashboardPageLayout";
}
