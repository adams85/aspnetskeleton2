/*
    This module is intentionally empty because the template project doesn't have a public UI (like a landing page) and
    the unrestricted pages (account-related pages mainly) doesn't use any scripts apart from the globally included ones.
*/

export { }
